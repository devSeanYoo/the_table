# THE TABLE — 개발 스펙 (최종본, 구현 완료)

> **상태: 구현 완료 · 테스트 완료.** 이 문서는 최초 작성된 개발 스펙에, 실제 빌드하면서 확정되거나 실사용 피드백에 따라 변경된 내용을 반영한 **최종 버전**입니다. 원본 스펙과 달라진 부분은 각 항목에 "**(변경)**" 또는 "**(신규)**"로 표시했습니다.

**참조 파일 (같은 프로젝트에 함께 제공):**
- `게임기획안_종합본.md` — 전체 게임 규칙(한국어, 권위 있는 원본)
- `THE_TABLE_국가카드_영문.md` — 9개국 카드 콘텐츠(영어, 학생 대면)
- `THE_TABLE_이벤트_영문.md` — 9개 이벤트 콘텐츠(영어, 학생 대면)
- `README.md` — 실행 방법, 폴더 구조 등 개발자용 상세 안내

---

## 0. 실행 방법 요약

로컬에서 Node.js가 전역 설치되어 있지 않아, `.tools/node/`에 포터블 버전을 두고 사용합니다.

```bash
# API 서버 (포트 4000) — Vercel 서버리스 함수와 동일한 핸들러 코드를 로컬에서 구동
"/Users/seanyoo/Downloads/The Table/.tools/node/bin/node" server/dev-server.js

# 클라이언트 (포트 5173)
"/Users/seanyoo/Downloads/The Table/.tools/node/bin/node" client/node_modules/vite/bin/vite.js client --port 5173
```

브라우저에서 `http://localhost:5173` 접속. 관리자 비밀번호 기본값은 `teacher123`(환경변수 `ADMIN_PASSWORD`로 변경 가능). Vercel + Upstash Redis 배포 방법은 README.md 참고.

---

## 1. 게임 개요 (변경 없음)

- 9개국, 2인 1팀(투르크메니스탄만 1인), 20분 × 10라운드
- 6종 자원(Oil, Gas, Mineral, Water, Food, Power)을 협상으로 거래
- 발전점수 = 트랙A(매 라운드 생존판정) + 트랙B(재투자, 간접기여)
- 국가별 패시브·액티브(Special Move)는 비공개, 자원 보유량도 비공개, 발전점수만 공개
- 화면 4개: 관리자 / 공용 대시보드 / 개인 페이지 / (브리핑 슬라이드는 별도, 웹앱 범위 아님)

---

## 2. 화면 구조 및 기능 (최종)

### 2-1. 관리자 페이지 (교사 전용)

**로그인**: 비밀번호 방식(스펙과 동일). 환경변수 `ADMIN_PASSWORD`.

| 기능 | 설명 |
|---|---|
| 🔑 Team Access PINs **(신규)** | 9개국 각각 4자리 PIN 자동 발급. 관리자가 라운드 진행 중 언제든 개별 재발급 가능("↺ New PIN"). 학생이 자기 나라가 아닌 남의 나라 페이지에 들어가지 못하도록 하는 접근 통제 장치 — 원 스펙은 "개인 페이지 비밀번호 불필요"였으나, 실사용 중 요청에 따라 추가됨 |
| Reveal Event | 해당 라운드 이벤트를 뉴스피드에 게시. **(변경)** 1~8라운드는 Start Timer보다 먼저 눌러야 함(원 스펙과 동일). 9·10라운드는 순서가 반대로 바뀜 — 6번 항목 참고 |
| Start / Pause / Reset Timer | 20분 타이머 제어, 전 화면 실시간 연동 |
| ⏭ Force End Round (debug) **(신규)** | 20분을 기다리지 않고 즉시 라운드를 종료 처리(거래 잠금 + 필요 시 채점까지). 수업 리허설·데모·장애 대응용 |
| Reveal Round 9 Results | 9라운드 비공개 투표 결과 공개 + 채점 실행 버튼. **(변경)** 활성화 시점이 원 스펙과 다름 — 6번 항목 참고 |
| ⏮ Round Checkpoints **(신규)** | 매 라운드가 시작될 때마다 전체 게임 상태(9개국 자원·점수·카드 사용 횟수·거래 내역 등 전부)가 자동으로 스냅샷 저장됨. 관리자가 임의 라운드 버튼을 눌러 그 시점으로 즉시 되돌릴 수 있음(확인창 필수). 되돌리면 그 이후 라운드의 스냅샷은 폐기됨. 실수로 잘못된 조작을 했거나 분쟁이 생겼을 때의 안전장치 |
| Full Country View | 9개국 전체 보유량 조회. **(확장)** 원 스펙 대비 각 자원의 "이번 라운드 생산량"과 "이번 라운드 요건(Req E/W/F)"도 함께 표시하여 디버깅·진행 편의성 향상 |
| Manual Override | 국가별 자원 6종, 점수, Special Move 잔여 횟수, 재투자(Build for the Future) 사용 횟수, 투르크메니스탄 가스 해금 여부를 각각 직접 입력하는 폼으로 구현 |
| Apply Language Penalty | 국가·자원·수량을 선택해 수동 차감 |
| Next Round | 다음 라운드로 전환. 9라운드에서는 Reveal Round 9 Results가 끝나야 활성화됨 |

### 2-2. 공용 대시보드 (프로젝터용)

원 스펙과 동일하게 구현: Round n of 10 / 20분 타이머 / Leaderboard(발전점수 순위만, 자원 비공개) / News Feed(이벤트 텍스트 + 공개형 Special Move 발동 알림).

### 2-3. 개인 페이지 (국가 선택, PIN 로그인)

**로그인 (변경)**: 국가를 선택한 뒤, 관리자에게 배포받은 4자리 PIN을 입력해야 입장 가능(원 스펙: 비밀번호 불필요 → 실사용 중 접근 통제 필요성 반영해 추가).

| 기능 | 설명 |
|---|---|
| 🎯 This Round, You Need **(신규)** | 이번 라운드 자국의 에너지/물/식량 요건 수치를 상단에 상시 표시. 단, 5·6·8라운드의 요건 변동분은 관리자가 해당 라운드 Reveal Event를 누르기 전까지는 반영되지 않아 스포일러가 되지 않음 |
| Your Country Card | 해당 국가 카드(플레이버 텍스트 + 패시브) 표시. **(변경)** 기본적으로 가려져 있고("🔒 Tap to reveal"), 학생이 직접 눌러야 노출됨 — 옆자리에서 훔쳐보는 것을 방지 |
| Your Resources | 자국 보유량 6종 표시. **(변경)** 기본 은닉 + 탭하여 열람. **(확장)** 보유량과 함께 이번 라운드 생산량("+N/rd")도 같이 표시 |
| Your Special Move **(신규 섹션)** | 자국 Special Move 이름·설명·사용 버튼. **(변경)** 기본 은닉 + 탭하여 열람. **(변경)** 게임 전체 사용 가능 횟수와 별개로, **라운드당 1회로 제한**(예: 에티오피아 Water Control은 전체 3회 가능하지만 한 라운드에는 1회만) |
| Build for the Future | 재투자 실행. 원 스펙과 동일 |
| Propose Trade / Trade Offers (Accept/Reject/Counter-Offer) | 원 스펙과 동일. **(보완)** 거래가 닫혀있는 국면(라운드 잠김 등)에는 버튼이 비활성화되어, 헛되이 시도하고 실패 메시지를 보는 대신 애초에 버튼이 눌리지 않도록 처리 |
| Round 9 전용 Comply / Ignore | 9라운드에서만 노출. **(변경)** 노출·제출 시점이 원 스펙과 다름 — 6번 항목 참고 |
| Trade History | 원 스펙과 동일 |

---

## 3. 학생에게 보여줄 규칙 설명 텍스트 — 변경 없음 (원 스펙 그대로 구현)

---

## 4. 국가별 자동 거래 규칙 — 변경 없음, 전부 구현 확인

| 국가 | 규칙 |
|---|---|
| Bolivia | 발신 거래마다 상대 수령량 = 보낸 양 − 1 (최소 0) |
| Ukraine | Food 거래는 3 이상 단위만 가능 |
| Venezuela | Power 보유 0이면 Oil 거래(발신) 불가 |
| Turkmenistan | 누적 Mineral **수령**(거래로 받은 양만 집계) 3 도달 시, 다음 라운드부터 Gas 자동산출 1→4 영구 변경 |
| Laos | 매 라운드 자동으로 Power 1을 Thailand에게 이전 |

라운드 7(에너지 대체 전쟁) 발동 중에는 Oil/Gas 거래에 한해 "보낸 양의 절반(내림)만 도착" 규칙이 위 규칙들과 함께 겹쳐 적용됨(예: Bolivia가 라운드7에 Gas를 보내면 2:1 비율 먼저 적용 후 Bolivia 패시브로 추가 -1).

---

## 5. Special Move 목록 — 확정 사항 포함

| 국가 | 이름 | 횟수(게임 전체) | 공개여부 | 비고 |
|---|---|---|---|---|
| Ethiopia | Water Control | 3 | Public | 고정 대상: Egypt |
| Egypt | Stop Ethiopia | 1 | Public | 반응형 — Ethiopia가 Water Control을 쓴 그 라운드 안에서만 취소 가능 |
| Laos | Emergency Release | 2 | Public | 고정 대상: Thailand. **(확정)** 라운드 2(메콩강 가뭄)로 라오스 전력 생산이 0이 된 라운드에는 이 카드를 사용할 수 없음(생산 자체가 없는데 방류할 것이 없다는 논리) |
| Thailand | Backup Plan | 1 | Private | 반응형 — 라오스의 피해만 취소, 라오스의 이득(+2)은 그대로 유지 |
| Turkmenistan | Block It | 1 | Public | **(확정)** 그 라운드에 발동된 Special Move 중 아무거나 선택해 무효화 가능(투르크메니스탄을 공격한 카드로 한정하지 않음, 폭넓게 구현). 단, 자동 무효화 로직은 Water Control / Emergency Release / Claim New Land 3종만 지원 — 그 외는 관리자 Manual Override로 수동 처리 |
| Venezuela | Claim New Land | 1 | Public | 자기 완결형, 대상 없음 |
| Bolivia | Take It Back | 1 | Public | 자신의 가장 최근 발신 거래 1건 자동 취소 |
| Ukraine | Special Deal | 1 | Public | 그 라운드 한정으로 Food 최소 3단위 규칙 해제 |
| Canada | Quick Swap | 3 | Private | 자원 1↔1 즉시교환 |

**(신규 규칙) 라운드당 1회 제한**: 위 횟수(게임 전체 사용 가능 횟수)와 별개로, 모든 Special Move는 **한 라운드에 한 번만** 사용할 수 있습니다. 예를 들어 에티오피아는 게임 전체에서 Water Control을 3번까지 쓸 수 있지만, 한 라운드 안에서 연달아 2번 쓸 수는 없습니다.

**Egypt "Stop Ethiopia" 타이밍 (확정)**: 사전 차단이 아니라, 에티오피아가 발동한 **직후** 취소하는 방식으로 구현됨. 에티오피아의 획득분(+2)과 이집트가 잃은 이번 라운드 생산분이 모두 원상 복구됨.

**Water Control의 "피해" 범위 (변경 — 실사용 중 발견된 버그 수정)**: 원 스펙 문구("Egypt loses all its Water and Power this round")를 처음에는 "이집트가 보유한 수자원·전력 전량을 0으로 만든다"로 구현했으나, 이는 라운드가 진행될수록(특히 여러 라운드에 걸쳐 비축한 자원이 쌓인 뒤에) 의도한 것보다 훨씬 과도한 피해가 되는 버그였음. **"this round" 표현의 의도는 "이번 라운드에 생산된 만큼만"으로 재해석**하여, 이제는 이집트의 **이번 라운드 수자원·전력 생산량만큼만** 차감하고(그마저도 실제 보유량을 초과해 차감하지 않음), 그 이전부터 비축해둔 자원은 그대로 유지되도록 수정함. Stop Ethiopia / Block It로 취소할 때도 정확히 그만큼만 되돌림(`server/src/engine/specialMoves.js`의 `waterLost`/`powerLost` 로그 필드 참고).

---

## 6. 라운드 흐름 (자동화 순서) — 9·10라운드 대폭 변경 확정

### 일반 라운드 (1~8) — 원 스펙과 동일

1. 관리자가 Reveal Event 클릭 → 뉴스피드 갱신 (**1~8라운드는 이 클릭 전엔 Start Timer 자체가 비활성화됨**)
2. 관리자가 Start Timer 클릭 → 20분 카운트다운 시작, 개인 페이지 거래 기능 활성화
3. (라운드 중) 학생들 자유롭게 거래·Special Move·재투자
4. 20분 종료(또는 Force End Round) → 자동으로 거래 잠김
5. 시스템이 트랙A 자동판정 → 점수 반영 → 소모 처리
6. 대시보드 Leaderboard 자동 갱신
7. 관리자가 Next Round 클릭 → 1번으로 반복

**영구효과 적용 시점(원 스펙 그대로 확정)**: 4라운드 종료 시 Canada·Laos Power 영구+1 자동 반영. 6라운드 Reveal Event 클릭 시 요건표(2/1/1→3/2/2) 영구 전환.

### 라운드 9 (죄수의 딜레마) — **(변경 확정)**

> 원 스펙: *"라운드 시작 시 예고 후 종료 직전 판정"*. 실사용 테스트 중 "처음부터 예고하면 학생들이 애초에 Oil/Gas 거래 자체에 소극적이 되어 협상의 재미가 줄어든다"는 피드백에 따라, **예고 시점을 라운드 종료 이후로 완전히 옮김.**

1. 관리자가 **Reveal Event 없이 바로 Start Timer 클릭** → 학생들은 이번 라운드에 특수 이벤트가 있다는 사실을 모른 채 평소처럼 자유롭게 협상
2. 20분 종료(또는 Force End Round) → 거래는 잠기지만, **아직 채점은 되지 않은 중간 상태("투표 대기")로 전환**
3. 관리자가 이 시점에 비로소 **Reveal Event** 클릭 → "The Climate Deal" 안내문이 공개되고, 그제서야 학생 개인 페이지에 **Comply / Ignore 버튼이 나타남**
4. 각국이 Comply 또는 Ignore를 비공개로 제출
5. 관리자가 **Reveal Round 9 Results** 클릭 → **이 클릭 한 번으로** 트랙A 채점(각국의 제출 선택 반영) + 6개국 이상 준수 여부 계산 + 그룹 보너스 지급이 **동시에** 처리됨. 이 클릭 전까지는 어떤 점수도 반영되지 않습니다.
6. Next Round는 5번이 완료되어야 활성화됨

### 라운드 10 (배터리 전환) — **(변경 확정)**

> 원 스펙: *"보유 핵심광물 1개당 즉시 +1점"*. "즉시"를 라운드 시작 시로 해석하면 학생들이 그 라운드 내내 광물만 긁어모으는 데 집중하게 되므로, "즉시"의 의도를 "관리자가 발표하는 그 순간 바로"로 재해석해 라운드 종료 후로 옮김.

1. 관리자가 **Reveal Event 없이 바로 Start Timer 클릭** → 평소처럼 협상
2. 20분 종료(또는 Force End Round) → 트랙A 자동판정·점수 반영(이 시점엔 배터리 붐 보너스는 아직 없음)
3. 관리자가 이 시점에 **Reveal Event** 클릭 → **그 즉시** 각국이 "현재 보유한" 핵심광물 개수만큼 추가 점수를 받음(광물은 소모되지 않음). 즉, 라운드10 마지막까지의 거래 결과가 그대로 반영된 최종 보유량 기준으로 채점됨

---

## 7. 확정된 결정 사항 (원래 "미해결 구현 세부사항"이었던 항목)

- **Egypt "Stop Ethiopia" 타이밍**: 발동 직후 취소 방식으로 확정 (5번 참고)
- **9번 이벤트 비공개 동시 제출 기능**: 구현 완료 — 서버가 각국 선택을 비공개로 보관하다가, 관리자의 Reveal Round 9 Results 클릭 시점에 한꺼번에 집계·공개
- **Manual Override UI**: 국가별 자원 6종 + 점수 + Special Move 잔여횟수 + 재투자 횟수 + 투르크메니스탄 가스해금 여부를 각각 직접 입력하는 폼으로 확정

---

## 8. 정보 은닉 정책 (최종)

| 공개 | 비공개(탭해야 보임) |
|---|---|
| 발전점수(Leaderboard) | 자원 보유량("Your Resources") |
| 이번 라운드 요건 수치("This Round, You Need") | 국가 카드 플레이버·패시브("Your Country Card") |
| 공개형 Special Move 발동 알림(국가명+이름만) | Special Move 상세 설명·사용 버튼("Your Special Move") |
| | 비공개형 Special Move(Thailand Backup Plan, Canada Quick Swap)는 발동해도 알림 없음 |

관리자 페이지는 예외적으로 전체 현황을 항상 조회 가능(진행·디버깅용).

---

## 9. 상태 저장 및 세션 재개 (신규 — 여러 날에 걸친 진행 지원, 서버리스 구조로 재구현)

수업을 하루에 다 끝내지 못하고 **여러 날에 나누어 진행**할 수 있도록, 서버는 **모든 행동(거래·Special Move·라운드 전환 등)마다 게임 전체 상태를 저장**합니다. 최초 구현은 로컬 파일(`server/data/save.json`)에 저장하는 방식이었으나, **Vercel 같은 서버리스 플랫폼에 배포하면 요청이 끝나는 즉시 파일 시스템이 초기화되어 로컬 파일 저장이 무의미해진다는 점**이 확인되어, 배포 환경에서는 **Upstash Redis**(요청 밖에서도 유지되는 외부 저장소)를 사용하도록 재구현했습니다 (`server/src/persistence.js`).

- **배포 환경(Vercel + Upstash Redis)**: 환경변수 `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`이 설정되어 있으면 자동으로 Redis를 사용합니다.
- **로컬 개발 환경**: 위 환경변수가 없으면 자동으로 `server/data/save.json` 파일에 저장합니다(기존 방식 그대로 유지 — 개발자가 Upstash 계정 없이도 바로 테스트 가능).
- 서버(또는 Vercel 함수)를 껐다가(며칠~몇 주 뒤 포함) 다시 켜도, **마지막으로 저장된 라운드·자원·점수·카드 사용 이력·거래 내역·팀 PIN·되돌리기(Round Checkpoints) 목록까지 전부 그대로** 이어집니다.
- 학생들은 나중에 다시 접속할 때도 **이전과 같은 국가 PIN**으로 로그인하면 자신의 마지막 상태를 그대로 볼 수 있습니다.
- 타이머는 "종료 시각(`endsAt`)"을 저장해두고, 상태를 요청할 때마다 "이미 끝났는가"만 확인하는 방식(지연 평가, `server/src/lazyTimer.js`)으로 재구현했습니다 — 서버리스 함수는 백그라운드에서 계속 실행되며 시간을 재는 것이 불가능하기 때문입니다. 결과적으로 동작은 기존과 동일합니다: 20분 중 5분 남기고 접속이 끊겼다가 2주 뒤에 다시 열어도, 이미 시간이 다 지났으므로 다음 상태 요청 시 즉시 라운드가 자동 종료 처리됩니다.

**실전 운영 팁**:
- 하루 일정을 마칠 때는 가급적 라운드가 깔끔하게 끝난 시점(다음 라운드 대기 중이거나, 타이머를 Pause한 상태)에서 종료하는 것을 권장합니다.
- Vercel 배포 시에는 상태가 Upstash Redis에 있으므로 특정 컴퓨터에 종속되지 않습니다 — 다른 컴퓨터에서 같은 배포 주소로 접속해도 이어집니다.
- 게임을 완전히 새로 시작하고 싶다면, 로컬은 `server/data/save.json` 파일을 삭제, 배포 환경은 Upstash 콘솔에서 `the-table:save` 키를 삭제한 뒤 다시 접속하면 됩니다.

---

## 10. 신규 추가된 안전장치 · 편의 기능 요약

원 스펙에는 없었지만, 실사용 준비 과정에서 추가로 확정·구현된 기능들:

1. **Team Access PINs** — 국가별 접근 통제
2. **정보 은닉 강화** — Country Card / Resources / Special Move 기본 가림 처리
3. **Round Checkpoints (되돌리기)** — 매 라운드 자동 스냅샷 + 임의 시점 복구
4. **Force End Round (디버그)** — 대기시간 없이 즉시 라운드 종료
5. **Special Move 라운드당 1회 제한**
6. **이벤트로 인한 생산 제한 시, 해당 자원을 만들어내는 Special Move 사용 차단** (예: 메콩강 가뭄 중 라오스 Emergency Release)
7. **이번 라운드 요건 상시 표시("This Round, You Need")**
8. **자원 생산량 표시("+N/rd")**
9. **9·10라운드 반전 이벤트 공개 타이밍** (6번 참고)
10. **상태 저장·세션 재개** (9번 참고)

---

## 11. 테스트 현황

`server/test/engine.test.mjs`에 28개의 자동화 유닛 테스트 포함 — 국가별 패시브·모든 Special Move·트랙A 채점·재투자 비용 곡선·라운드 4/6/9/10 특수 처리·되돌리기 기능을 전부 커버. 전체 10라운드를 실제 관리자 화면으로 처음부터 끝까지 진행하는 통합 테스트, PIN 재접속 테스트, 서버 재시작 후 상태 복구 테스트까지 모두 통과 확인.

---

## 12. 알려진 제한 사항

- Vercel + Upstash Redis 배포 방법은 README.md에 정리되어 있으나, 실제 계정 생성·배포는 사용자가 직접 진행해야 함(에이전트가 대신 계정을 만들 수 없음).
- 배포 시 `ADMIN_PASSWORD` 환경변수를 기본값(`teacher123`)에서 반드시 변경 권장.
- Upstash 무료 티어에는 요청 수·용량 제한이 있으나(수업 규모의 폴링 트래픽에는 충분), 별도 백업 기능은 없으므로 장기간 중요한 진행 상황은 관리자 화면에서 상태를 캡처해두는 것을 권장.
- 뉴스피드·거래내역 등은 게임 도중 계속 누적되며 자동으로 정리되지 않음(10라운드 한 세션 기준으로는 용량 문제가 없는 수준).
- 클라이언트는 2초 주기로 상태를 폴링함 — 실시간 소켓 방식보다 반응 속도가 약간(최대 2초) 느릴 수 있으나, 이 게임의 턴/라운드 성격상 체감상 문제가 되지 않는 것으로 확인.
